import psycopg2
 
conn = psycopg2.connect(
    host="c-fosa-cosmospostgresql-nonprod.7ka34v5i3uwhhc.postgres.cosmos.azure.com",
    port=5432,
    database="fosa",
    user="citus",
    password="K@i5eP05t"
)
 
print("Connected successfully!")
 
cursor = conn.cursor()
 
# Test query
cursor.execute("SELECT version();")
result = cursor.fetchone()
print("DB Version:", result)
 
cursor.close()
conn.close()
 
